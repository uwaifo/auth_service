package main


func main() {
	//data := data2.SampleUser
	//keyPem := utils.ReadFile("./jwtRS256.key")
	//keyPemPub := utils.ReadFile("./jwtRS256.key.pub")
	//expires := time.Now().Add(24 * time.Hour)

	//token := auth.CreateRSA256SignedToken(keyPem, data, expires, {})
	//fmt.Println(token)

	//claims := auth.ParseRSA256SignedToken(token, keyPemPub)

	//fmt.Println(utils.Stringify(claims))
	//fmt.Println(keyPem)
	//fmt.Println(data)
	//fmt.Println(expires.Unix())
}
